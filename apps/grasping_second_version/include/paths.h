#pragma once

#include "paths.h"
#include <string>

namespace paths {
const std::string folder_path = "apps/grasping_second_version/parameters/";

const std::string setpoint_path = folder_path + "setpoint.yaml";

const std::string setpoint_list_path = folder_path + "setpoints.csv";

const std::string parameters_path = folder_path + "parameters.yaml";
} // namespace paths
