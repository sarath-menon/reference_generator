#pragma once

namespace prm {

// Step size in degrees
float step_size = 0.1;

// center angle in degrees
float theta = 0;

// Radius of circle to be tracked (in meters)
float radius = 1.5;

// Local center
float center_x = 0;
float center_y = 0;

// constant altitude
float altitude = 1.5;
} // namespace prm