#pragma once

#include "paths.h"
#include <string>

namespace paths {
const std::string folder_path = "apps/grasping_first_version/parameters/";

const std::string waypoint_path = folder_path + "waypoint.yaml";

const std::string waypoint_list_path = folder_path + "waypoints.csv";

const std::string parameters_path = folder_path + "parameters.yaml";
} // namespace paths
