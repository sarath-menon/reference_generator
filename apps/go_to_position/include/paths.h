#pragma once

#include "paths.h"
#include <string>

namespace paths {
const std::string folder_path = "apps/go_to_position/parameters/";

const std::string setpoint_path = folder_path + "setpoint.yaml";
} // namespace paths
